import { useState } from 'react';
export function RegisterPage({onSubmit,onBack}:{onSubmit:(v:{brandName:string;clientId:string;clientPassword:string;adminPassword:string})=>void;onBack:()=>void;}){const [f,set]=useState({brandName:'',clientId:'',clientPassword:'',adminPassword:''}); return <div className='min-h-screen flex items-center justify-center'><div className='bg-white p-6 rounded shadow w-full max-w-md space-y-3'><h1 className='text-xl font-bold'>가입</h1>{Object.entries(f).map(([k,v])=><input key={k} className='border p-2 w-full' placeholder={k} type={k.includes('Password')?'password':'text'} value={v} onChange={e=>set({...f,[k]:e.target.value})}/>) }<button className='bg-green-600 text-white px-3 py-2 rounded w-full' onClick={()=>onSubmit(f)}>가입 완료</button><button className='underline text-sm' onClick={onBack}>로그인으로</button></div></div>}
