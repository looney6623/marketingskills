import { Channel, NaverType } from '../types/models';
export function Sidebar({channel,setChannel,naverType,setNaverType}:{channel:Channel;setChannel:(c:Channel)=>void;naverType:NaverType;setNaverType:(n:NaverType)=>void;}){return <aside className='w-56 bg-white border-r p-4 space-y-2'><button onClick={()=>setChannel('all')}>전체</button><button onClick={()=>setChannel('naver')}>네이버</button><button onClick={()=>setChannel('meta')}>메타</button>{channel==='naver'&&<div className='pl-2 space-y-1'>{(['all','sa','da','gfa'] as NaverType[]).map(n=><button key={n} className={naverType===n?'font-bold':''} onClick={()=>setNaverType(n)}>{n.toUpperCase()}</button>)}</div>}</aside>}
